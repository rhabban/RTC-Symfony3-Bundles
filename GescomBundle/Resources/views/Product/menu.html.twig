<ul class="nav nav-pills nav-stacked">
    {% for product in listProducts %}
        <li>
            <a href="{{ path('cch_gescom_product_view', {'id': product.id}) }}">
                {{ product.name }}
            </a>
        </li>
    {% endfor %}
</ul>